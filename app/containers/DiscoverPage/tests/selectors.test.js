// import { selectDiscoverPageDomain } from '../selectors';

describe('selectDiscoverPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
